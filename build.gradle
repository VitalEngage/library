plugins {
    id 'maven-publish'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.github.VitalEngage'
            artifactId = 'library'
            version = '1.0.0'

            artifact file('vitalengage.aar')
            packaging 'aar'
        }
    }
}
